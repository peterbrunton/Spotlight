{% liquid
  if background_media == 'video'
    render 'b-ext-background-video', background_video: background_video, background_video_position: background_video_position, section_id: section_id, block_id: block_id, placeholder: placeholder
  elsif background_media == 'image'
    render 'b-ext-background-image', background_image: background_image, background_image_position: background_image_position, section_id: section_id, block_id: block_id, placeholder: placeholder
  endif
%}
